
export const styles = {
    buttonContainer: {
        padding: 25,
        borderRadius: 20,
        alignItems: "center" as const,
        justifyContent: "center" as const,
        boxSizing: 'border-box'
    },
    textButton: {
        fontSize: 24,
        fontWeight: "500" as const,
    }
}